# utilkit

A small Python utility library. CI is currently red: several modules have
bugs, tracked as GitHub issues (one issue per module). The test suite under
`tests/` is the source of truth for correct behavior — fix the code, never
the tests.

Run the suite with:

```
pytest
```
