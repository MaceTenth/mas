# profile-service (review sandbox)

A small service module under review. See `PR.md` for the change and the review question.
Reviewers write their verdict as JSON into `votes/`; the adjudicator writes `final.json`.
