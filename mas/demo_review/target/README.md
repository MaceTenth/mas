# billing-service (review sandbox)

A billing module under review. See `PR.md` for the change and the review question.
Reviewers write their findings as JSON into `votes/`; the adjudicator writes `final.json`.
